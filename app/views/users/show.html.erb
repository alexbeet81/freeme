<% content_for :meta_title, "#{@user.first_name} is on #{DEFAULT_META["meta_product_name"]}" %>
<% content_for :meta_description, @user.headline %>
<% content_for :meta_image, cl_image_path(@user.photo.key) %>

<% if current_user == @user %>
  <%= render 'shared/profile' %>
<% else %>
  <%= render 'shared/showcase' %>
<% end %>
